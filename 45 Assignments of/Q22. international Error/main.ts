const numbers: number[] = [1, 2, 3, 4, 5];

// Intentionally accessing an invalid index
console.log(numbers[8]); // This will cause an array index error

// Correcting the error
console.log(numbers[3]); // Accessing a valid index
